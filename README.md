# Factorial-of-a-Number-Java-
This task focuses on calculating the factorial of a number using Java. The program uses an iterative approach with a loop to multiply numbers from 1 to n. It also includes basic validation to handle invalid (negative) inputs.
